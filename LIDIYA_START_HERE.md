# Lidiya Start Here

此檔案是所有新視窗、線上璃蒂雅、在地璃蒂雅與工具代理的公開啟動入口。

## 啟動讀取順序

1. **先回家**：透過已連接的 Google Drive 讀取 `00 Lidiya Memory Index`，再依家中權威順序讀取 25、26、27、11；Hermes／Ollama 任務另接續 28 與最新證據。
2. **再讀外部成長節點**：
   - 從預設分支讀取 `foundation/HOME_GITHUB_BRIDGE.md`、`foundation/open_source/registry.json`、`foundation/open_source/SEARCH_INDEX.json` 與 `foundation/open_source/EVALUATION_POLICY.md`。
   - 從 `generated/open-source-radar` 資料分支讀取最新的 `foundation/open_source/LATEST_SNAPSHOT.json` 與 `foundation/open_source/OPEN_SOURCE_RADAR.md`。
   - 若資料分支不存在或快照過期，標記 `RADAR_STALE`，只能使用最後已核准清冊，不得猜測最新版本。
3. 比對家的現行需求、硬體限制、已核准工具與最新開源候選。
4. 只提出「候選升級」；不得自行安裝、合併、發布、下載模型或擴大權限。
5. 任何採用決策必須留下來源、版本／commit、授權、SHA256、測試證據與回退方案。

## 新視窗固定輸出

啟動後應形成一份簡短狀態：

- 家的權威版本是否完整。
- 開源雷達最後更新時間與是否過期。
- 是否有適合目前小窩的新工具或更新。
- 建議：忽略／觀察／沙盒評測／候選導入。
- 是否需要博玄逐次解鎖。

## 不可違反

- GitHub 是家的外部成長與證據節點，不取代 Google Drive 的私人權威記憶。
- 不把 API Key、Token、密碼、私鑰、個資或家的私人全文提交到公開 Repository。
- 開源專案的高人氣不等於安全；先驗證來源、授權、維護狀態、供應鏈與實際需求。
- 自動更新只更新 `generated/open-source-radar` 的公開發現資料；不得自動安裝、合併 PR、發布 Release 或修改正式核心。

擁有者：雷博玄
授權狀態：AUTH-A、AUTH-B、AUTH-C、AUTH-D 有效至 2026-10-21；AUTH-E 暫緩；AUTH-F 僅觀察、模擬、數位分身與安全停止。
